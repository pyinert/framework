# PyWeave Framework Skeleton

Default application skeleton for PyWeave.

GitHub repository: `pyweave/framework`  
PyPI package: none

This repository is copied by `pyweave/installer` when users run:

```bash
pyweave new my-app
```
