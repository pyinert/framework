[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "A PyWeave app"
requires-python = ">=3.10"
dependencies = [
  "pyweave",
  "loom",
  "pyweave-spa",
  "pyweave-pwa",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
