[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "A PyInert app"
requires-python = ">=3.10"
dependencies = ["pyinert", "pyinert-spa", "pyinert-pwa"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
